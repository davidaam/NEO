with
	var x <- 4,w: int
	var y: char
	var z: char
begin
	x <- -3.
	y <- 'a'++.
	z <- 'a'--.
	w <- #'a'.

	with
		var a <- true+true: bool
	begin
		a <- 4.
		for i from 1 to 7 ->
			a <- adkasj.
		end
	end
end
